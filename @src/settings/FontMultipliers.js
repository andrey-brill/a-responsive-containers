

// FontSize=360px is height of capital letters
// LineHeight=360px mean that font placed perfectly into container with height 360px
const Roboto = {
    fx: 506 / 360,
    lx: 366 / 360
};

export const FontMultipliers = {
    default: Roboto,
    Roboto
};
